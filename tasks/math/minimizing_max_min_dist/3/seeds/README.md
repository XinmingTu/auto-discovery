# minimizing_max_min_dist/3 seeds

This directory stores a small read-only seed portfolio for the 3D min-max distance task.

Use it to preserve different point-set construction families across runs while keeping a single mutable working file: `solve.py`.
