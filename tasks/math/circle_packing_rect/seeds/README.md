# circle_packing_rect seeds

This directory stores a small read-only seed portfolio for `circle_packing_rect`.

Use it to preserve different rectangle-packing construction families across runs while keeping a single mutable working file: `solve.py`.
